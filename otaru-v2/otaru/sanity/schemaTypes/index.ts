import { chapter } from "./chapter";
import { journal } from "./journal";
import { materialSpec } from "./materialSpec";
import { studioPage } from "./studioPage";

export const schemaTypes = [chapter, journal, materialSpec, studioPage];
